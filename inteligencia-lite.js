/* Inteligência Lite v3 — capacidade e horas calculadas no mesmo período */
(function(){'use strict';const B=window.BH;if(!B)return;async function load(){await B.ready;if(!window.user||!window.sb)return;const now=new Date(),start=new Date(now.getFullYear(),now.getMonth(),now.getDate()),end=new Date(start);end.setDate(end.getDate()+30);const startIso=start.toISOString().slice(0,10),endIso=end.toISOString().slice(0,10),[p,a,c]=await Promise.all([window.sb.from('profiles').select('id,name,hours_per_day,days_per_week,active').eq('active',true),window.sb.from('agenda_items').select('profile_id,collaborator_name,date,hours,status').gte('date',startIso).lte('date',endIso).limit(2000),window.sb.from('commissions').select('id,name,status,deadline,progress').not('status','eq','Concluído').order('deadline',{ascending:true,nullsFirst:false}).limit(20)]);if(p.error||a.error||c.error){console.warn('Bithouse Inteligência:',p.error||a.error||c.error);return}const ps=p.data||[],as=a.data||[],cs=c.data||[],used=new Map();as.filter(x=>B.norm(x.status)!=='concluido'&&B.norm(x.status)!=='cancelado').forEach(x=>{const key=x.profile_id||B.norm(x.collaborator_name);used.set(key,(used.get(key)||0)+Math.max(0,Number(x.hours||0)))});const factor=30/7,rows=ps.map(x=>{const weekly=Number(x.hours_per_day||0)*Number(x.days_per_week||0),cap=weekly*factor*.8,u=used.get(x.id)||used.get(B.norm(x.name))||0;return{name:x.name,cap,u,free:Math.max(0,cap-u),pct:cap?Math.min(1,u/cap):0}}),total=rows.reduce((n,x)=>n+x.cap,0),u=rows.reduce((n,x)=>n+x.u,0),pct=total?Math.min(1,u/total):0;const set=(id,v)=>{const e=B.q(id);if(e)e.textContent=v};set('#committedHours',u.toFixed(1)+'h');set('#freeHours',Math.max(0,total-u).toFixed(1)+'h');const capEl=B.q('#capacityInsights'),planEl=B.q('#planningInsights');if(capEl)capEl.innerHTML='<div class="cards"><article class="card"><span class="eyebrow">CAPACIDADE · 30 DIAS</span><h3>'+Math.round(pct*100)+'%</h3><div class="muted">ocupação planejada</div></article><article class="card"><span class="eyebrow">HORAS COMPROMETIDAS</span><h3>'+u.toFixed(1)+'h</h3><div class="muted">mesmo período da capacidade</div></article><article class="card"><span class="eyebrow">HORAS LIVRES</span><h3>'+Math.max(0,total-u).toFixed(1)+'h</h3><div class="muted">capacidade estimada</div></article></div><div class="cards" style="margin-top:14px">'+rows.map(x=>'<article class="card"><h3>'+B.esc(x.name)+'</h3><div class="muted">'+x.u.toFixed(1)+'h usadas · '+x.free.toFixed(1)+'h livres</div><div class="bar"><span style="width:'+Math.round(x.pct*100)+'%"></span></div></article>').join('')+'</div>';if(planEl)planEl.innerHTML='<div class="card" style="margin-top:14px"><span class="eyebrow">PRÓXIMOS PRAZOS</span>'+(cs.length?cs.slice(0,8).map(x=>'<div class="row"><span>'+B.esc(x.name)+'</span><strong>'+B.fmt(x.deadline)+'</strong></div>').join(''):'<div class="muted">Nenhum prazo pendente.</div>')+'</div>'}load()})();