/* BITHOUSE — PHASE 7: SMART PLANNER | on-demand */
(function(){'use strict';const sb=()=>window.sb,n=v=>Number(v||0),norm=v=>String(v||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().trim(),key=d=>new Date(d+'T12:00:00').toISOString().slice(0,10);const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));function addDay(s,n){const d=new Date(s+'T12:00:00');d.setDate(d.getDate()+n);return key(d)}function isWorkday(s){return new Date(s+'T12:00:00').getDay()!==0}function canonical(v){const x=norm(v);return x==='concluido'?'Concluído':x==='em andamento'?'Em andamento':x==='bloqueado'?'Bloqueado':'Não iniciado'}function dailyCapacity(p){return n(p.hours_per_day)*.8}function specialtyScore(p,step){const a=norm(p.specialty||p.role),b=norm(step.step_type);if(!a||!b)return 0;if(a.includes(b)||b.includes(a))return 100;return b.split(/\s+/).filter(x=>x.length>3).some(w=>a.includes(w))?50:0}
async function fetchPlannerData(){const today=key(new Date()),future=addDay(today,30);const [pr,ag,st]=await Promise.all([sb().from('profiles').select('id,name,specialty,role,hours_per_day,days_per_week').eq('active',true),sb().from('agenda_items').select('id,profile_id,date,hours,status,asset_step_id').gte('date',today).lte('date',future),sb().from('asset_steps').select('id,step_type,status,assigned_to,estimated_minutes,planned_minutes,priority,planned_date,depends_on_step_id').neq('status','Concluído').limit(500)]);return{profiles:pr.data||[],agenda:ag.data||[],steps:st.data||[]}}
function buildPlans(d){const today=key(new Date()),usedByDay=new Map();d.agenda.forEach(a=>{if(canonical(a.status)==='Concluído')return;const k=a.profile_id+'|'+a.date;usedByDay.set(k,(usedByDay.get(k)||0)+n(a.hours))});const assigned=new Set(d.agenda.map(a=>a.asset_step_id).filter(Boolean));const steps=d.steps.filter(s=>!assigned.has(s.id)&&canonical(s.status)!=='Concluído');return steps.map(step=>{const mins=n(step.estimated_minutes)||n(step.planned_minutes);if(!mins)return null;const candidates=d.profiles.map(p=>{const daily=dailyCapacity(p);let best=null;for(let i=0;i<30;i++){const day=addDay(today,i);if(!isWorkday(day))continue;const used=usedByDay.get(p.id+'|'+day)||0,room=Math.max(0,daily-used);if(room>=Math.min(daily,mins/60)){best={day,room};break}}return{p,day:best?.day||null,room:best?.room||0,score:(best?best.room*10:0)+specialtyScore(p,step)}).filter(x=>x.day).sort((a,b)=>b.score-a.score);const pick=candidates[0];return{step,suggested:pick?.p||null,date:pick?.day||step.planned_date||null,minutes:mins,reason:pick?'melhor equilíbrio entre capacidade e especialidade':'sem capacidade disponível'}}).filter(Boolean)}
async function applyPlan(plan){if(!plan?.suggested)return{ok:false,message:'Sem responsável disponível.'};const st=plan.step,p=plan.suggested,up=await sb().from('asset_steps').update({assigned_to:p.id,planned_date:plan.date||null,updated_at:new Date().toISOString()}).eq('id',st.id);if(up.error)return{ok:false,message:up.error.message};const existing=await sb().from('agenda_items').select('id').eq('asset_step_id',st.id).maybeSingle();if(existing.data){const au=await sb().from('agenda_items').update({profile_id:p.id,date:plan.date,status:'Não iniciado',hours:Math.round(plan.minutes/60*100)/100}).eq('id',existing.data.id);if(au.error)return{ok:false,message:au.error.message}}await sb().from('activity_log').insert({actor:window.user?.id||null,action:'smart_plan_applied',entity_type:'asset_step',entity_id:st.id,metadata:{assigned_to:p.id,planned_date:plan.date}});return{ok:true,message:'Plano aplicado.'}}
async function autoPlanAll(){if(!sb()||!window.user)return;const d=await fetchPlannerData(),plans=buildPlans(d);window.bithousePhase7={data:d,plans,generatedAt:new Date().toISOString()};render(plans)}
function render(plans){let host=document.querySelector('#planningInsights');if(!host)return;document.querySelector('#bh7Panel')?.remove();const box=document.createElement('div');box.id='bh7Panel';box.className='bh7-panel';const ready=plans.filter(x=>x.suggested).length;box.innerHTML=`<div class="bh7-head"><div><small>PLANEJADOR INTELIGENTE</small><h3>Distribuição recomendada</h3><p>${ready} de ${plans.length} etapas podem receber uma recomendação.</p></div><button class="ghost-btn small" id="bh7Run">↻ Recalcular</button></div><div class="bh7-actions"><button class="primary-btn" id="bh7ApplyAll">Aplicar recomendações</button><span class="bh7-note">Nada é movido sem esta ação.</span></div><div class="bh7-list">${plans.slice(0,30).map((x,i)=>`<div class="bh7-row"><div><b>${esc(x.step.step_type||'Etapa')}</b><small>${x.minutes} min • ${esc(x.date||'sem data')} • ${esc(x.reason)}</small></div><strong>${x.suggested?'→ '+esc(x.suggested.name):'Sem candidato'}</strong><button class="ghost-btn small" data-bh7="${i}" ${x.suggested?'':'disabled'}>Aplicar</button></div>`).join('')||'<div class="muted">Nenhuma etapa nova para planejar.</div>'}</div>`;host.appendChild(box);box.querySelector('#bh7Run').onclick=autoPlanAll;box.querySelectorAll('[data-bh7]').forEach(b=>b.onclick=async()=>{b.disabled=true;const r=await applyPlan(plans[Number(b.dataset.bh7)]);b.textContent=r.ok?'✓ Aplicado':'Erro';if(r.ok)autoPlanAll();else alert(r.message)});box.querySelector('#bh7ApplyAll').onclick=async()=>{const b=box.querySelector('#bh7ApplyAll');b.disabled=true;b.textContent='Aplicando...';let ok=0;for(const plan of plans){if(plan.suggested&&(await applyPlan(plan)).ok)ok++}b.textContent=`✓ ${ok} aplicadas`;setTimeout(autoPlanAll,300)}}
function injectStyle(){if(document.querySelector('#bh7Style'))return;const s=document.createElement('style');s.id='bh7Style';s.textContent='.bh7-panel{margin-top:16px;background:#fff;border:1px solid #d9e1ef;border-radius:20px;padding:18px}.bh7-head{display:flex;justify-content:space-between;gap:14px;align-items:start}.bh7-head h3{font:700 22px Space Grotesk;margin:5px 0}.bh7-head p{margin:0;color:#71809a;font-size:12px}.bh7-head small{font-size:10px;font-weight:900;color:#58709b;letter-spacing:1.5px}.bh7-actions{display:flex;align-items:center;gap:10px;flex-wrap:wrap;margin:15px 0}.bh7-note{font-size:11px;color:#71809a}.bh7-list{display:grid;gap:7px}.bh7-row{display:grid;grid-template-columns:1fr auto auto;gap:10px;align-items:center;border:1px solid #e5e9f1;border-radius:12px;padding:10px}.bh7-row b{display:block;font-size:12px}.bh7-row small{display:block;color:#7b89a1;font-size:10px;margin-top:3px}@media(max-width:650px){.bh7-panel{padding:14px}.bh7-head{flex-direction:column}.bh7-row{grid-template-columns:1fr}.bh7-row button{width:100%}.bh7-actions .primary-btn{width:100%}}';document.head.appendChild(s)}
function init(){injectStyle()}window.loadSmartPlanner=autoPlanAll;window.bithousePhase7={refresh:autoPlanAll,apply:applyPlan};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();})();