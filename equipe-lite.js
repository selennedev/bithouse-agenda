/* Equipe Lite */
(function(){'use strict';const B=window.BH,s=window.sb,$=B.q;async function load(){const r=await s.from('profiles').select('id,name,specialty,role,hours_per_day,days_per_week,active').order('name');const g=$('#teamGrid');g.innerHTML='';(r.data||[]).forEach(p=>{const cap=Number(p.hours_per_day||0)*Number(p.days_per_week||0)*.8;const el=document.createElement('article');el.className='card';el.innerHTML='<h3>'+B.esc(p.name||'Membro')+'</h3><div class="muted">'+B.esc(p.specialty||p.role||'Equipe')+'</div><div style="font:800 30px Space Grotesk;margin-top:12px">'+cap.toFixed(1)+'h <span class="muted">de capacidade semanal</span></div><div class="muted">'+(p.active===false?'Inativo':'Ativo')+'</div>';g.appendChild(el)})}setTimeout(load,250)})();