/* BITHOUSE — PHASE 4.3/4.4: workload and allocation intelligence */
(function(){'use strict';
const sb=()=>window.sb,n=v=>Number(v||0),norm=v=>String(v||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().trim();
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
async function loadPlanning(){if(!sb()||!window.user)return;const [p,a,s]=await Promise.all([sb().from('profiles').select('*').eq('active',true),sb().from('agenda_items').select('profile_id,date,hours,status'),sb().from('asset_steps').select('id,status,assigned_to,estimated_minutes,planned_minutes,priority,step_type')]);const profiles=p.data||[],agenda=a.data||[],steps=s.data||[],today=new Date().toISOString().slice(0,10);
const workload=profiles.map(x=>{const daily=n(x.hours_per_day)*.8,hours=agenda.filter(a=>a.profile_id===x.id&&a.date>=today&&norm(a.status)!=='concluido').reduce((z,a)=>z+n(a.hours),0),cap=daily*14;return {...x,committed_hours:hours,daily_capacity:daily,utilization:cap?hours/cap:0,open_slots:Math.max(0,cap-hours)}});
const suggestions=steps.filter(s=>norm(s.status)!=='concluido'&&!s.assigned_to).map(step=>{const type=norm(step.step_type);const candidates=workload.filter(p=>p.daily_capacity>0).map(p=>({...p,score:p.open_slots+(norm(p.specialty||p.role).includes(type)?1000:0)})).sort((a,b)=>b.score-a.score);return {step,suggested:candidates[0]||null}});
const risks=workload.filter(p=>p.utilization>=.9).map(p=>({severity:p.utilization>=1?'danger':'warning',message:`${p.name}: ${Math.round(p.utilization*100)}% da capacidade projetada.`}));window.bithousePlanning={workload,suggestions,risks,generatedAt:new Date().toISOString()};render(workload,suggestions,risks)}
function render(w,s,r){const h=document.querySelector('#planningInsights');if(!h)return;h.innerHTML=`<div class="planning-grid"><div class="planning-card"><small>EQUIPE</small><strong>${w.length}</strong><span>pessoas ativas</span></div><div class="planning-card"><small>SOBRECARGA</small><strong>${w.filter(x=>x.utilization>=1).length}</strong><span>acima da capacidade</span></div><div class="planning-card"><small>ETAPAS SEM RESPONSÁVEL</small><strong>${s.length}</strong><span>sugestões</span></div><div class="planning-card"><small>ALERTAS</small><strong>${r.length}</strong><span>atenção</span></div></div><div class="planning-columns"><div><h3>Distribuição recomendada</h3>${s.slice(0,12).map(x=>`<div class="planning-row"><b>${esc(x.step.step_type||'Etapa')}</b><span>${x.step.estimated_minutes||x.step.planned_minutes||0} min</span><em>${x.suggested?'→ '+esc(x.suggested.name):'Sem candidato'}</em></div>`).join('')||'<div class="muted">Todas as etapas já possuem responsável.</div>'}</div><div><h3>Alertas</h3>${r.map(x=>`<div class="planning-alert ${x.severity}">⚠ ${esc(x.message)}</div>`).join('')||'<div class="muted">Nenhum alerta.</div>'}</div></div>`}
window.loadPlanning=loadPlanning;if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',loadPlanning);else loadPlanning();
})();
